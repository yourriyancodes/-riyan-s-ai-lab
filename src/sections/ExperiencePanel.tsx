import PanelShell from './PanelShell'
import ExperienceBody from './ExperienceBody'

/** EXPERIENCE — career path through the universe. */
export default function ExperiencePanel() {
  return (
    <PanelShell label="CAREER PATH" wide>
      <ExperienceBody />
    </PanelShell>
  )
}
