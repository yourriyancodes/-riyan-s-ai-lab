{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "target": "ES6",
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "//exclude": "The Vite application in src/ plus its config are orphaned (their dependencies are not declared in package.json). They stay on disk untouched, but are excluded so `next build` type-checks the ACTIVE App Router app instead of failing on dead code. Re-include them if the Vite app is ever revived.",
  "exclude": [
    "node_modules",
    "src",
    "vite.config.ts"
  ]
}
